import React from './package/react';
import ReactDOM from './package/react-dom';
const element = (
    <div>
        hello world! 
    </div>
);

ReactDOM.render(
    element,
    document.getElementById('root')
)